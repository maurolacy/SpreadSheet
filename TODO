- Support more operators
  Logarithms (log, log10, log2, ln)
  Trigonometric functions (sin, cos, tan)
  Inverse trigonometric functions (asin, acos, atan)
  ...

- Improve caching
  - Use a caching crate (cached or similar)
  - Cache operations as well, not only cells (caching as part of the AST)

- Add support for the formula (^=) syntax
  - Literal (string) if not formula
  - Add support for strings
  - Add support for string operations
    - String concatenation (through CONCAT)
    - ...
  - This means, variable return types
    - Discriminate between string, int and float results

- Stress test the sheet with many cells

- Turn the pending bullet points into GitHub issues
